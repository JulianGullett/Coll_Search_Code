import random

num_agents = 16
num_searches = 2
num_graphs = 1
num_sims = 1
distance_costs = [0]
edges = [0]

memory=[[(15,10), (1,0)]
       ]
# memory_ = []
# for each in memory:
#     memory_ += [each[1]]*each[0]
# memory = memory_
# random.shuffle(memory)
